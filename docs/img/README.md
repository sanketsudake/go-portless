# Diagram sources

PNGs here are embedded in the top-level README (mermaid does not render on
pkg.go.dev). Regenerate after editing a `.mmd` source:

```sh
npx @mermaid-js/mermaid-cli -i dial-path.mmd -o dial-path.png -b white -w 1100 -s 2
```
